{
  "name": "tinysearch",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}